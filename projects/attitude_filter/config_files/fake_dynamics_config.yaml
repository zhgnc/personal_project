# ---- Telemetry: recording rate and level (off / required / debug) ----
tlm_rate_hz: 5.0
tlm_level: debug

# ---- Model parameters ----
initial_quaternion: [0.0, 0.0, 0.0, 1.0]

x_axis_period_s: 30.0
x_axis_amplitude_dps: 3.0
x_axis_shift_deg: 0.0

y_axis_period_s: 30.0
y_axis_amplitude_dps: 3.0
y_axis_shift_deg: 90.0

z_axis_period_s: 30.0
z_axis_amplitude_dps: 3.0
z_axis_shift_deg: 0.0